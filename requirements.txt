# CairnIQ — Direct Dependencies
# Uses >= minimum pins so pip can resolve compatible versions
# across Python 3.12–3.13 and any platform (macOS, Linux, Windows).
# (numpy>=2.5 dropped 3.11 entirely; pyarrow>=24 ships wheels for 3.11–3.13
# but numpy is now the tighter floor — 3.10 and 3.11 are no longer supported.)

# --- Web Server ---
fastapi>=0.135.0
uvicorn>=0.49.0
python-multipart>=0.0.12
starlette>=1.2.1

# --- Auth (multi-user login → token → profile) ---
# Pure-Python; HS256 token signing uses stdlib hmac, so no native build deps.
PyJWT>=2.13.0

# --- LLM / Agent Framework ---
langchain>=1.3.10
langchain-core>=1.4.8
langchain-aws>=1.6.0
langchain-openai>=1.3.0
langchain-anthropic>=1.4.6
langchain-community>=0.4.2
langchain-text-splitters>=1.0.0
langgraph>=1.0.0
langgraph-checkpoint>=4.1.1
langgraph-checkpoint-sqlite>=3.0.0
langgraph-prebuilt>=1.0.0
langgraph-sdk>=0.4.2
langsmith>=0.8.6
litellm>=1.70.0

# --- DSPy ---
dspy>=3.0.0

# --- AWS ---
boto3>=1.43.27
botocore>=1.35.0

# --- Data & Finance ---
yfinance>=1.4.1
pandas>=3.0.3
numpy>=2.5.1
scipy>=1.11.0
ta>=0.11.0
matplotlib>=3.11.0
plotly>=6.0.0

# --- Web Scraping & Search ---
beautifulsoup4>=4.15.0
lxml>=6.1.1
requests>=2.34.2
httpx>=0.27.0
html2text>=2025.4.15
ddgs>=9.14.4
defusedxml>=0.7.0

# --- NLP & Tokenization ---
tiktoken>=0.7.0

# --- Serialization ---
orjson>=3.10.0
pydantic>=2.7.0
pydantic-settings>=2.0.0

# --- Utilities ---
python-dotenv>=1.0.0
# Cross-platform OS-keychain access for API key storage:
#   macOS  -> Keychain Access (Security framework)
#   Windows-> Credential Manager (DPAPI)
#   Linux  -> Secret Service (gracefully falls back to .env if unavailable)
keyring>=24.0.0
rich>=15.0.0
tqdm>=4.68.3
tenacity>=9.1.4
pytest>=9.1.1
# Parallel test execution. `make test` and CI run `pytest -n auto`, taking the
# ~1100-test suite from ~50s serial to ~30s across cores.
pytest-xdist>=3.8.0
networkx>=3.0
pyvis>=0.3.0
backoff>=2.2.1
cachetools>=7.1.4
colorlog>=6.10.1
psutil>=5.9.0
portalocker>=3.2.0
asciichartpy>=1.5.0
certifi>=2023.0.0
Jinja2>=3.1.6
Pillow>=10.0.0
pypdf>=5.0.0
python-docx>=1.2.0
openpyxl>=3.1.5

# --- Data Formats ---
pyarrow>=24.0.0

# --- Hugging Face & optional semantic retrieval support ---
huggingface_hub>=1.0.0
tokenizers>=0.20.0
rank_bm25>=0.2.2

# --- SQLAlchemy (LangGraph checkpoints) ---
SQLAlchemy>=2.0.0
aiosqlite>=0.20.0
